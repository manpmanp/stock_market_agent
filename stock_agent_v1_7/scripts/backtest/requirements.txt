# Python deps for scripts/backtest/harness.py.
# tabpfn pulls in torch and is a large download on first install -- that's
# expected, not a bug, per the "Model Selection & Testing" methodology tab.
# If tabpfn fails to install or import, harness.py degrades gracefully and
# still runs the linear + GBM comparison; it just skips TabPFN and says so.
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
tabpfn>=2.0
